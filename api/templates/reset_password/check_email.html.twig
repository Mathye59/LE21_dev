{% extends 'auth/_layout.html.twig' %}
{% block title %}E-mail envoyé — Le 21{% endblock %}

{% block auth_content %}
  <div class="form-wrap">
    <div class="alert">
      Si un compte correspond à <strong>{{ email }}</strong>, un e-mail contenant un lien de réinitialisation vient d’être envoyé.
      <br>Le lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </div>

    <p class="help">
      Vous n’avez rien reçu ? Vérifiez votre dossier <em>indésirables</em> ou
      <a href="{{ path('app_forgot_password_request') }}">réessayez</a>.
    </p>

    <div style="margin-top:16px">
      <a class="btn btn-ghost" href="{{ path('app_login') }}">Retour à la connexion</a>
    </div>
  </div>
{% endblock %}
